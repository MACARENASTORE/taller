export default {
    /**
     * @swagger
     *
     * /formulario/{formulario}:
     *   get:
     *     summary: Obtener definición de formulario
     *     parameters:
     *       - in: path
     *         name: formulario
     *         required: true
     *         schema:
     *           type: string
     *     responses:
     *       200:
     *         description: Definición de formulario obtenida exitosamente
     *
     */
  };
  